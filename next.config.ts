import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable React Strict Mode for highlighting potential issues during development
  reactStrictMode: true,
};

export default nextConfig;
